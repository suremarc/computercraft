apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cc-web-gateway
spec:
  gatewayClassName: cilium
  listeners:
  - protocol: HTTP
    port: 80
    name: cc-web-gateway
    allowedRoutes:
      namespaces:
        from: All
